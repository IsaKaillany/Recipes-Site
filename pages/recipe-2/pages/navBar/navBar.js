import styles from "../styles/navBar.module.css";

export default function NavBar() {
    return (
        <nav className={styles.navBar}>
            <h1>FRAMEWORK NEXT.JS</h1>
        </nav>
    )
}